# run python3 -m doctest -v ./tests/1-my_list.txt


Import
>>> MyList = __import__('1-my_list').MyList

SUCCESS CASES:

Test ints (all elements will be type int)
     >>> my_list = MyList()
     >>> my_list.append(1)
     >>> my_list.append(0)
     >>> my_list.append(-1)
     >>> print(my_list)
     [1, 0, -1]
     >>> my_list.print_sorted()
     [-1, 0, 1]
     >>> print(my_list)
     [1, 0, -1]
     >>> list = MyList()
     >>> list.append(-1)
     >>> print(list)
     [-1]
     >>> list.print_sorted()
     [-1]
     >>> print(list)
     [-1]
     >>> list = MyList()
     >>> print(list)
     []
     >>> list.print_sorted()
     []
     >>> my_list = MyList()
     >>> isinstance(my_list, object)
     True
     >>> my_list = MyList()
     >>> isinstance(my_list, list)
     True
     >>> my_list = MyList()
     >>> issubclass(MyList, list)
     True
     >>> my_list = MyList()
     >>> type(my_list) == MyList
     True
